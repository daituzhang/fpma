{% set backgroundImage = block.backgroundImage|length == 0 ? "" : " background: url(" ~  block.backgroundImage|first.url ~ ") no-repeat;   background-size: 100%;" %}

<section class="specials" style="{{ backgroundImage }}">
  <div class="container">
    {% if block.heading %}
      <header>
        <h2>{{ block.heading }}</h2>
      </header>
    {% endif %}
    {% if block.body %}
        {{ block.body }}
    {% endif %}
    {% if children|length %}
      <div class="grid">
        {% for child in block.specials %}
          {% if child.blocks|length %}
          <div class="col col-1-of-2 col--m-1-of-2">
            <div class="home-location-box">
              <div class="box-content">
                <b>{{ child.title }}</b>
                {{ child.body }}
              </div>
            </div>
          </div>
          {% endif %}
        {% endfor %}
      </div>
    {% endif %}
  </div>
</section>